[pytest]
markers =
    slow: long-running tests kept out of the default quick suite
    integration: cross-surface or browser/node/integration tests kept out of the default quick suite
    exhaustive: broad alias matrices and comprehensive regressions kept out of the default quick suite
    docs: README and documentation sanity checks
    timeout: per-test timeout marker used by pytest-timeout when installed
